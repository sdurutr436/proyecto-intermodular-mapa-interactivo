# ============================================
# Variables de entorno del Backend
# ============================================
# Este archivo debe copiarse como .env en /backend
# Comando: cp .env.example .env

# MONGODB CONFIGURATION
# URI de conexión a la base de datos MongoDB
# Para Docker: mongodb://mongo:27017/translator_db
# Para MongoDB local: mongodb://localhost:27017/translator_db
# Para MongoDB Atlas: mongodb+srv://usuario:password@cluster.mongodb.net/translator_db
MONGO_URI=mongodb://mongo:27017/translator_db

# SERVER CONFIGURATION
# Puerto en el que escuchará el servidor backend
# Valor por defecto: 5000
# IMPORTANTE: Si cambias esto, actualiza también docker-compose.yml
PORT=5000

# ENVIRONMENT MODE
# Entorno de ejecución de la aplicación
# Valores posibles: development | production | test
# En desarrollo: muestra logs detallados y errores completos
# En producción: optimiza rendimiento y oculta detalles de errores
NODE_ENV=development
